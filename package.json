{
  "name": "xuru-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tracks Xuru Stays availability + per-night rates for BlueKeys (partner sync).",
  "engines": { "node": ">=22" },
  "scripts": {
    "catalogue": "node src/catalogue.js",
    "sweep": "node src/sweep-months.js"
  }
}
